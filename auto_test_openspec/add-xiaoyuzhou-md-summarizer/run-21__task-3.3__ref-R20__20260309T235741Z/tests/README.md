This validation bundle uses CLI/static checks only (compile + grep/pattern checks).
No audio fixtures are included because Whisper model weights are large and transcription is nondeterministic across environments.
